import Link from 'next/link'; import { prisma } from '@/lib/prisma'; import { deletePost } from '../../actions';
export default async function PostsPage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;const items=await prisma.post.findMany({where:{title:q.search?{contains:q.search,mode:'insensitive'}:undefined,status:q.status==='DRAFT'||q.status==='PUBLISHED'?q.status:undefined},include:{category:true},orderBy:{updatedAt:'desc'}});return <><header className="admin-page-head"><div><span>EDITORIAL</span><h1>Articles</h1></div><Link className="admin-primary" href="/admin/posts/new">New article</Link></header><form className="admin-filter"><input name="search" placeholder="Search articles" defaultValue={q.search}/><select name="status" defaultValue={q.status||''}><option value="">All statuses</option><option>DRAFT</option><option>PUBLISHED</option></select><button>Filter</button></form><div className="admin-table-wrap"><table><thead><tr><th>Title</th><th>Category</th><th>Status</th><th>Updated</th><th/></tr></thead><tbody>{items.map(p=><tr key={p.id}><td><strong>{p.title}</strong><small>/{p.slug}</small></td><td>{p.category.name}</td><td><span className={`admin-pill ${p.status.toLowerCase()}`}>{p.status}</span></td><td>{p.updatedAt.toLocaleDateString()}</td><td className="admin-row-actions"><Link href={`/admin/posts/${p.id}`}>Edit</Link><form action={deletePost}><input type="hidden" name="id" value={p.id}/><button>Delete</button></form></td></tr>)}</tbody></table></div></>}

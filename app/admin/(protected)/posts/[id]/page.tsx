import { notFound } from 'next/navigation'; import { prisma } from '@/lib/prisma'; import PostForm from '../../../post-form';
export default async function EditPost({params}:{params:Promise<{id:string}>}){const {id}=await params;const [post,categories]=await Promise.all([prisma.post.findUnique({where:{id}}),prisma.category.findMany({where:{type:'POST'},select:{id:true,name:true},orderBy:{sortOrder:'asc'}})]);if(!post)notFound();return <><header className="admin-page-head"><div><span>EDITORIAL</span><h1>Edit article</h1></div></header><PostForm categories={categories} post={post}/></>}

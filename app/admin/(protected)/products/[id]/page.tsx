import{notFound}from'next/navigation';import{prisma}from'@/lib/prisma';import ProductForm from'../../../product-form';
export default async function EditProduct({params}:{params:Promise<{id:string}>}){const{id}=await params;const[product,categories]=await Promise.all([prisma.product.findUnique({where:{id}}),prisma.category.findMany({where:{type:'PRODUCT'},select:{id:true,name:true},orderBy:{sortOrder:'asc'}})]);if(!product)notFound();return <><header className="admin-page-head"><div><span>CATALOG</span><h1>Edit product</h1></div></header><ProductForm categories={categories} product={product}/></>}
